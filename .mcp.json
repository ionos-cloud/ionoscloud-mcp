{
  "mcpServers": {
    "ionoscloud": {
      "command": "npx",
      "args": [
        "-y",
        "@smithery/cli",
        "run",
        "@ionos-cloud/ionoscloud-mcp",
        "--config",
        "{\"ionos_token\":\"${IONOS_TOKEN}\"}"
      ]
    }
  }
}
